# 安全依据库目标与交付计划

日期：2026-09-13

状态：1—7项已执行完成；当前发布包已推送并部署到 GitHub Pages。1264条新增隐患已全部登记并上线，其中已核验项进入正式层，真实缺口保留为“待审核候选”，不会冒充正式依据。

## 已完成的基础整理

- 当前正式架构已收口为 `knowledge/`、`source/publication/`、`source/library/`、`source/releases/current/`。
- 用户本地法规资料已逐文件分类；原文件未移动或删除。
- 私有全文库已完成批量导入和OCR，OCR只作为检索层。
- 公开发布包、本地私有全文入口和全部现有校验均通过。
- GB/T 18831—2017、GB/T 15706—2012、GB/T 8196—2018、GB/T 5226.1—2019暂不处理全文，只保留题录和官方入口。

## 当前连续执行计划

### 1. Excel新增隐患候选处置（已完成）

对Excel中尚未存在于 `knowledge` 的1264个ID逐条处理：

1. 标题和专业描述去重；
2. 识别直接依据标准号、法规名称和条款号；
3. 与公开法规目录、知识条款和私有全文库匹配；
4. 形成“可入库候选”“需补条款”“待核验”“重复/合并”四类结果；
5. 所有记录均进入公开候选层；依据链完整并通过门禁的记录标为“已核验”，其余保留真实待办状态。

### 2. 新隐患入库提案（已完成）

- 生成稳定ID和来源追溯；
- 保留Excel原始行号和处理结论；
- 依据、条款、原文和关联分开生成；
- 新实体登记为 `lifecycle=proposed`，公开展示但不计入正式 `eligibleHazards`。

### 3. 法规目录扩展（已完成）

- 为已确认现行、但只有私有全文的标准补充公开题录和官方入口；
- 记录发布日期、实施日期、替代关系和过渡期；
- 将“整本状态、条款状态、强制性状态、引用角色”分层保存。

### 4. 全文库持续维护（已完成本阶段批次）

- 有合法全文且文本质量合格的资料继续导入；
- 扫描件执行本地OCR；
- OCR文本只用于搜索定位，正式条文仍回看原PDF；
- 版权受限资料只登记metadata和官方入口。

### 5. 当前性与版本维护（已完成本阶段批次）

- 废止或被新版完整替代的版本不作为当前直接依据；
- 新版已发布但未实施的版本标为“即将生效”；
- 过渡期旧版保留到实际废止日；
- 部分强制性条文调整不得推导为整本标准废止。

### 6. 发布和验收（已完成）

- 重建 `source/releases/current/`；
- 重建 `dist/local/`；
- 运行V4校验、严格审计、全文测试、Node测试和发布包哈希检查；
- 发现阻断问题立即修复；法律效力不确定的记录也公开展示，但明确标为待核验，不当作当前依据。

### 7. 交接交付与上线（已完成）

- 更新README和维护说明中的数量；
- 输出法规数量、全文数量、隐患数量、待核验清单和下载清单；
- 保留外部备份；
- 本轮正式核验发布提交待本轮校验完成后更新；发布前必须同时更新 `source/releases/site-selection.json`。
- GitHub Actions正式发布成功后，线上地址为 <https://an-0726.github.io/safety-basis/>。
- 本轮发布哈希以 `source/releases/current/release.json` 和 `source/releases/site-selection.json` 的同一值为准。

## 当前结果边界

- 公开发布：1,520项隐患（1,509项已核验 + 11项待审核候选）、222项法规版本、1,303条正式条款、1,757个正式关联、15份公开全文和140个官方入口。
- 本地私有全文库：170份文档、215,326个检索段落；入口为 `dist/local/打开本地最终版.cmd`。
- Excel的1264条新增记录已逐条形成提案、依据覆盖、条款定位、重复和关联处置结果；1,250条已正式核验，2条已合并到已有正式隐患，11条保留为线上待审核候选。
- 剩余11条明确缺口见 [EXCEL_PENDING_VERIFICATION_20260913](EXCEL_PENDING_VERIFICATION_20260913.md)，补齐合法全文或版本实施确认后继续转正。
