# V4 / V3 差异（已并入 `V3_V4_DIFF_REPORT.md`）

本文件是早期中途快照，统计口径与最终版不同、数字已过时，不再维护。

最终 V3 → V4 差异验收结论见 **[`docs/V3_V4_DIFF_REPORT.md`](V3_V4_DIFF_REPORT.md)**，
由 `py tools/v4/diff_v3_v4.py` 在运行时从只读 V3 库与 `knowledge/` 实时生成，全部计数无硬编码。

保留本文件仅为避免旧链接失效。
