# 待审核候选处理规则

本文件是当前唯一的候选处理规则。历史批次数字和旧处理记录不在主分支保留，需追溯时查看 Git 历史。

## 1. 候选的含义

`lifecycle=proposed` 表示记录已经进入结构化知识源，但法规版本、具体条款、原文证据、适用性或去重结论尚未全部满足正式门禁。

候选不是正式法律依据，也不是法规效力状态。候选不得因为“已经有名称/条款要点/搜索摘要”就转为正式。

常见 `proposalStatus`：

- `workbook_revised_pending_clause_rebind`：字段已修订，待重新绑定精确条款和证据；
- `basis_catalog_only`：只有法规题录或官方入口，没有可核验具体条款；
- `basis_name_unresolved`：法规身份、编号或版次尚未确认；
- `same_title_review`：与已有隐患同名或高度相似，必须先判断合并还是保留。

## 2. 当前发布规则

- `knowledge/` 保留候选，防止未完成工作丢失；
- `source/releases/current/` 和 GitHub Pages **不发布 proposed 候选**；
- 正式发布包只包含当前 `asOf` 日期链式 Gate 通过的 `active` 隐患；
- 候选如需查看，应在本地审核工具/知识源中查看，不再通过正式公共站的“状态筛选”暴露；
- 重复/合并记录不得重复发布为另一条当前隐患；
- 候选退出正式包不等于删除，原 ID、来源行、待办状态仍保留在 `knowledge/`。

## 3. 转正固定流程

1. 确认隐患对象、场景和专业描述，先做重复/合并判断；
2. 确认法规或标准的唯一身份、编号和适用版本；
3. 核对发布日期、实施日期、废止/替代关系；
4. 从官方原文或合法保存的原始 PDF 中定位具体条、款、项/标准条号；
5. 建立或复用 `knowledge/clauses/` 条款，保存逐字原文和来源；
6. 建立证据、条款审核、隐患审核和直接关联审核；
7. 只有完整 Gate 通过后，才把候选转为 `active`；
8. 重新构建、校验并更新 README 当前基线。

## 4. 时间效力规则

- `active` 且已经到实施日的版本，才可以支撑“当前正式依据”；
- `upcoming` 可以保留并提前研究，但实施日前不能支撑当前正式隐患；
- 实施日到达后必须重新核对效力、替代关系和条款适用性，不能仅靠日期自动转正；
- 已废止或已过 `endDate` 的版本只用于历史追溯。

## 5. 禁止事项

- 不把 AI 摘要、搜索摘要、Excel“依据条款要点”当成法规原文；
- 不仅凭文件名或标准名称判断现行性；
- 不把尚未实施的新版本自动当作当前现行版本；
- 不因标题相似直接删除，必须比较对象、场景、整改措施和依据；
- 不为了前台整洁重编号已有稳定 ID；
- 不把候选数量报成正式已核验数量。

## 6. 当前数据口径

截至 2026-09-17，PHASE 6 已对原 519 条 `proposed` 候选完成 **519/519** 最终处置：其中 20 条在法规身份、现行版本、逐字条款原文、对象适用性和审核链全部满足后转为 `active`；其余 **499 条**因证据、对象、条件、数值口径、条文质量或适用性不足继续保持 `proposed`，并保留明确原因和下一证据动作。

2026-09-18 的 GB/T 47236-2026 专题批次又复核 17 条候选：16 条在现行版本、官方逐字原文、对象语义、设备适用范围和审核哈希完整后转为 `active`；1 条因与既有 active 隐患仅部分重叠，继续 `proposed` 等待拆分或合并方案。机器明细见 `docs/phase11-gbt47236-disposition.jsonl`。

当前 `knowledge/` lifecycle 为 **1,458 active / 471 proposed / 86 superseded**。正式公共站只统计当前 Gate 通过的 active 投影，`proposed` 公网数量必须保持 **0**；不得把后台 backlog 加到正式发布总数，也不得为了清零 backlog 降低证据/审核门槛。
