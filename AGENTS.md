# safety-basis 工作规则

## 当前任务优先
- 只执行用户本次授权的任务和范围；读取本仓库、审计规则或修正文档不等于恢复业务项目。
- 项目阶段与已核事实在 [PROJECT_STATE](docs/PROJECT_STATE.md)。只有接续阶段或需要业务基线时读取；它是记录与计划，不是自动执行指令。
- 项目目前暂停。用户明确恢复后，核对实际状态并完成授权范围；不要完成一个动作后自动推进全部路线图。
- 局部目标完成且相关检查通过后结束。不因等待某项证据或CI暂停独立工作；无新信息不重复检查。

## 证据与数据
- 正式法规事实回到官方原文/原始证据和 `knowledge/`。AI、搜索、OCR、Excel摘要只能用于定位；准确原文不足时标待核，不编造。
- 正式引用链保留法规身份、适用版本、具体条款、原文、证据和适用性审核；最新发布不等于当前适用。
- `knowledge/` 是正式结构化知识源；`source/library/` 是本地私有证据，archive默认不可变；SQLite是检索库，不高于原始法律证据。
- `source/publication/` 只承担来源；生成包、网页索引和 `dist/` 可重建，不得反向当母库。
- `proposed` 不进入正式公网；`upcoming` 不提前支持当前正式隐患。不为了候选清零凑证据。
- 不为整洁重编号 LF/LV/C/H 稳定ID；同名不等于同版本，归并优先alias/canonical mapping并保留来源。
- 不提交私有PDF、SQLite、OCR、企业资料或 `source/releases/current/` 生成包。不按标题相似删除法规/隐患。

## 写入边界
- 已授权的只读核验、代码/文档修改和副本验证可直接做；用户明确修正不重复确认。
- 写工作SQLite、删除/移动archive、合并documents、删除正式实体、修改稳定ID或批量正式关联时，先确认该操作属于授权范围，做一致性备份及相关验证。
- 不在SQLite被写入时替换文件。documents/全文摘要/段落数量意外变化、FTS不一致或来源链失联时停止相关写入并调查，必要时从已验证备份恢复。
- 异构SQLite运行时结果不能单独触发覆盖工作库；结合项目本机运行时、内容不变量及搜索抽检验收。

## Git与验证
- 先检查工作区，保留现有修改及同步临时目录。通常从最新main建分支；接续已授权但未合并的工作时保留该分支基线，不重置用户进度。
- 纯文档改动检查内容、相对链接及差异；涉及代码/数据时运行受影响的测试、Gate和构建。检查通过后不无理由重复全量测试。
- 发布任务才要求PR/CI通过、合并、Pages部署和线上验收；不得把这些作为文档修订或只读审计的完成条件。
- 仅实际阶段进展更新 PROJECT_STATE；架构/口径改变才同步README，交接方式变化才同步HANDOFF。不把每个微小动作都变成记忆文件改写。
- 只有确需跨环境协作时读 [执行协议](AGENT_EXECUTION_PROTOCOL.md)。具备本机工具时直接执行授权工作，不按模型名字限制普通能力。
