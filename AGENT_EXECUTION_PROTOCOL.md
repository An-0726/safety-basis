# 跨环境执行与验收

## 何时使用
仅当当前任务确实需要其他环境或执行器时读取。本文件不记录当前项目阶段；阶段与暂停状态见 [PROJECT_STATE](docs/PROJECT_STATE.md)。

## 分工
- 当前主执行者负责范围、专业判断、证据标准及最终验收。执行位置和模型名称不自动决定能否承担判断。
- 当前工具能安全访问本机时，直接完成已授权的文件、Git、备份和验证操作。
- 仅当必要的本机状态或操作无法由当前工具可靠完成时，使用可用执行器；没有可调用执行器才给用户一段可复制提示词。
- 提示词包含目标、允许范围、已知基线、停止条件及验收要求。不要把无关工作一并转交。

## 回传与复核
- 执行结果包含实际命令、结果和有关证据。主执行者核对是否满足本次验收标准；仅缺乏可信证据或高风险状态需要时独立复测，不机械重跑所有步骤。
- 等待回传时继续独立工作；不得声称未执行步骤已完成，也不无限轮询。外部阻塞明确报告已完成部分及恢复条件。
- SQLite、archive、稳定ID及正式法规事实继续遵守 AGENTS.md 的授权、备份和证据边界。
- 本次授权范围验收后结束；推进其他阶段须属于用户明确的恢复范围。

## 历史记录
旧协议中的PHASE 3、旧分支和FTS修复提示词是历史执行记录，可从Git追溯，不作为当前待办或等待条件。
